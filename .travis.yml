sudo: required
dist: trusty
language: node_js
node_js:
  - node

services:
  - postgresql

before_script:
  - psql -c 'create database postit_test;' -U postgres
  - npm install sequelize-cli -g

script:
  - npm test  
env:
    global:
        -NODE_ENV=test

       